## Summary
Briefly describe the change and why it is needed.

## Changes
- [ ] Includes tests or updates existing tests
- [ ] Updates docs / README / CHANGELOG
- [ ] Passes `make ci` locally (`ruff check`, `ruff format --check`, `mypy`, `pytest --cov`)

## How to verify
Commands run, e.g.:
```bash
uv run ruff check .
uv run ruff format --check .
uv run mypy
uv run --extra dashboard --extra tracking pytest --cov
```

## Screenshots / logs (if UI or CLI)

## Related issues
Closes #
